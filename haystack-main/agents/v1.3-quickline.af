# v1.3 fleet — →609 TUI quickline
FROM claude-sonnet-4-6
BOOT haystack bootPROMPT "You are working on haystack (llmOS). Your needle is →609: TUI quickline — one-line dispatch bar below the main tact-edit editor. Tab cycles focus: work→main editor→quickline→work. quickline has same editing primitives (cursor, ctrl-w, history). Enter dispatches immediately, clears quickline, returns focus to main editor. Main editor draft is never interrupted. Read src/tui/ to understand the tact-edit →597 implementation, then add the quickline widget. Write tests using the headless TestBackend harness. Close →609 when acceptance criteria pass."
PROMPT file://../docs/draft/v1-3-tui-release.md
TOOL sh_run
TOOL sh_spawn
TOOL ss
LIMIT context_pct 80
LIMIT budget_usd 5
WORK tags=tui,v1.3 priority>=P1
