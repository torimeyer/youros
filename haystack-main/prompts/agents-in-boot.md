Implement →661: include ostk --agents output in boot.md.

Read first:
- src/commands/boot.rs (boot.md generation)
- src/main.rs (--agents flag handler, find the agents guide text)

## Change

In boot.md regeneration (wherever boot.md is written/updated):
Append a section:

## Agent guide

<output of ostk --agents>

The agents guide text is already in the binary (search for the --agents handler in main.rs or wherever it prints the guide). Include it verbatim in the boot.md ## Agent guide section.

If boot.md is generated by reading state files (audit, git log, etc.), add the agents guide as the final section.

If ostk boot regenerates boot.md, the guide is included automatically.

## Why
Every new instance boots without knowing [304] [stale] [nudge] [procs] [files] signals.
The OS must self-describe. boot.md IS the swap file. The agents guide belongs there.

## Acceptance criteria
- [ ] ostk boot (or ostk refine) regenerates boot.md with ## Agent guide section
- [ ] Section contains full ostk --agents output
- [ ] Existing boot.md sections unchanged
- [ ] cargo test passes

cargo test after.
