# Shared Python traceback patterns

[[rules]]
# Python traceback frames
pattern = '^\s+File ".+", line \d+'
action = "dedup"
description = "Python stack frame"

[[rules]]
# The code line after "File" line in tracebacks
pattern = '^\s{4}\S'
action = "dedup"
description = "Python traceback code line"
