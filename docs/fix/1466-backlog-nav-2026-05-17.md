# →1466 Backlog nav consolidation

## Summary

Combines Specs + Tasks into a single **Backlog** left-nav item linking to a new `/backlog` page with three sub-tabs: All, Specs, Tasks.

## Current step

GREEN phase for Sidebar tests, then Backlog page RED→GREEN, then routes.

## Fixes required

1. `app/src/stores/app.ts`: Replace `Tasks` + `Specs` entries in `DEFAULT_FEATURES` with `{ label: 'Backlog', enabled: true }`.
2. `app/src/components/Sidebar.tsx`: Change `TOP_LEVEL_ROUTES` from `/tasks` to `/backlog`.
3. `app/src/pages/Backlog.tsx`: New page with sub-tab routing.
4. `app/src/App.tsx`: Add `/backlog`, `/backlog/specs`, `/backlog/tasks` routes; redirect old `/tasks` and `/specs`.

## Parent scaffold commit

`4c9ed1a` — scaffold RED tests + partial Sidebar impl already committed to main.

## Agent

execute-wreath-plan-1466-30fd70 | 2026-05-17
