# v4.1.0 Cut Prep Audit

**Date:** 2026-05-28
**Agent:** wave-3-audit-worktree-dry-run-br-21bb83
**Purpose:** Pre-release audit for v4.1.0 cut. Two parts: worktree quiesce dry-run (→1775) and branch diff stat for `fix-1768-calendar-font`.

---

## Part A: Worktree Quiesce Dry-Run (→1775)

### Command run

```
bash scripts/worktree-reaper.sh   # no --apply flag (dry-run)
```

### Counts

| Metric | Value |
|--------|-------|
| `git worktree list` entries | 203 |
| Dirs under `.claude/worktrees/` | 187 |
| Worktrees inspected by reaper | 170 |
| Classified **absorbed** (would be deleted on --apply) | **0** |
| Classified **unique** (kept regardless) | **170** |
| Dirty (uncommitted work, skipped) | ~15 |
| Orphan dirs on disk (no git registration) | 0 |

The discrepancy between `git worktree list` (203) and reaper-inspected (170) is because:
- 1 is the main worktree
- Several are registered in git but lack a corresponding `.claude/worktrees/` directory (stale git refs)

### Absorbed sample entries

**None.** The reaper found zero absorbed worktrees. Every single worktree has either unmerged commits, uncommitted work, or was explicitly refused deletion.

### Unique sample entries (representative cross-section)

**1. `worktree-agent-1338-http-https-probe-22f8c807`**
- Classification: unique (2 unmerged commits ahead of main)
- Reaper: REFUSED to delete — cherry-pick required first
- Key commit: `47e7eab5 fix(→1338): migrate http probe callsites to https`

**2. `worktree-agent-1600-phase-3-spec-uni-2477a9d9`**
- Classification: unique (dirty — uncommitted work present)
- Reaper: skipped (uncommitted changes)
- State: active working tree with in-progress changes

**3. `worktree-agent-audit-adopt-claude-co-a6d09a4f`**
- Classification: unique (53 unmerged commits ahead of main)
- Reaper: REFUSED to delete
- Largest worktree by commit count; contains breakroom art passes, dashboard fixes, and multiple merge commits

**4. `worktree-agent-phase-3a-tone-js-fanf-403a00ac`**
- Classification: unique (2 commits)
- Key commits: `20fcf4d1 feat(fanfare): play Gemini-composed .mid on needle close via Tone.js`
- Unmerged Tone.js/MIDI fanfare feature

**5. `worktree-agent-b2-1765-receipts-rege-7430866d`**
- Classification: unique (dirty — uncommitted work)
- Reaper: skipped
- Active receipts-gate work in progress

### Recommendation: **DO NOT APPLY**

Running `--apply` is safe from a data-loss perspective (absorbed=0, so nothing would actually be deleted). However, the recommendation is still **don't apply** for v4.1.0:

1. **Zero cleanup value.** With 0 absorbed worktrees, `--apply` deletes nothing. There is no disk space or branch-list clutter to recover.
2. **~15 dirty worktrees need triage first.** Several worktrees have uncommitted changes that haven't been reviewed. These should be saved, committed to their branches, or explicitly discarded before any reaper pass.
3. **53 worktrees with >1 unmerged commit.** Several large worktrees (53 commits, 45+ commits) represent substantial work that has never landed on main. Run a cherry-pick or merge pass before any destructive sweep.
4. **→1775 is still a P1 action item**, not a v4.1.0 blocker. The worktree list is noisy but not unsafe. Do the dry-run again after the pending dirty worktrees are resolved.

---

## Part B: Branch Diff Stat Audit — `fix-1768-calendar-font`

### Commit count

```
git log --oneline origin/main..fix-1768-calendar-font | wc -l
```

**65 commits** ahead of `origin/main`.

> Note: The branch name `fix-1768-calendar-font` predates its current scope. It has grown far beyond a calendar font fix and now carries 65 commits of multi-feature work.

### File change summary

```
131 files changed, 3198 insertions(+), 4487 deletions(-)
```

Net: **-1,289 lines** (primarily rename/refactor passes removing more than they add, consistent with needle→task vocabulary sweep).

### Commit categories

| Type | Count (approx) | Notable work |
|------|----------------|--------------|
| **rename** | 6 | needle→task phases A-F (→1788/→1789): full vocabulary rename across frontend, backend, hooks, docs |
| **merge** | ~15 | Squash merges from individual feature worktrees |
| **fix** | ~15 | Event-loop blockers (→1778), Vite 8 blank page (→1798), chat tab isolation (→1782), sidebar overlay (→1780), async I/O (→1783) |
| **feat** | ~8 | ToriChat semantic search (→1787), Jira inline comment (→1772), launchd KeepAlive (→1744), brainstorming skill override (→1791), context_lines search (→1786) |
| **docs** | ~10 | Plain-language rules guide (→1732), pre-design-audit verification (→1790), backend surface map (→1789), needle-to-task rename map (→1794) |
| **chore/scaffold/wip** | ~6 | Scaffolds, wip commits |
| **test** | ~5 | Stale test fixes, myOS→yourOS pattern updates |

### First 20 commits (most recent)

```
d3bc8246 rename(phase F): docs sweep + README needle → task
e1a47478 rename(phase E): CLAUDE.md vocabulary needles → tasks
d9bcc229 rename(phase D): needle_hygiene → task_hygiene + hook injection text
c86f4f61 merge(→1799): test already green — needle stale
021e0418 rename(→1789): phase C — set_needle_in_progress → set_task_in_progress
01a9ce9c fix(→1799): Layout chat-resize test already green — needle stale
0ca7a7ae rename(→1788/→1789): phase B — kind='needle' API param + SpecWizard internals
e7a0ad47 merge(→1744): activate launchd KeepAlive backend + kill-only watchdog
c3f44029 merge(→1772): re-surface Jira inline Comment on detail page
b79fc0d4 merge(→1706): Discord connected service spec
f96ea1d5 merge(→1798): vite 8 blank page fix
200171d1 rename(→1788): frontend phase A — user-facing needle→task + drop →1736 metaphor
9e9b16db feat(→1772): re-surface Jira inline Comment action on Jira detail page
85311bee feat(→1744): activate launchd KeepAlive backend + kill-only watchdog
3a3dd1e9 docs(→1706): spec Discord as a connected service
1b474ead fix(→1798): cache nuke + ignoreOutdatedRequests + holdUntilCrawlEnd to fix Vite 8 blank page
ca9ee289 docs(→1777): vite 504 root cause report — CJS chunk hash divergence
f484e51a fix(→1755): cache Drive service + parallelize cold path metadata+export
89e024fb feat(→1787): expose mcp__ostk__search as semantic_search tool for ToriChat
9121e470 docs(→1732): plain-language rules guide for non-engineers
```

### Needle-tagged commits (→NNN pattern, top 40)

All 65 commits include needle tags. Needles referenced: →1744, →1772, →1706, →1798, →1788, →1789, →1799, →1777, →1755, →1787, →1786, →1785, →1795, →1796, →1797, →1793, →1792, →1791, →1790, →1789, →1794, →1784, →1783, →1782, →1781, →1780, →1778, →1740, →1777, →1782, →1775 (via rename passes)

### Suggested release notes headlines for v4.1.0

1. **Tasks everywhere.** The "needle" vocabulary has been replaced with "task" across the full stack — frontend labels, backend API params, hooks, and all docs. (→1788, →1789, phases A-F)

2. **Backend stays up.** launchd KeepAlive means the yourOS backend restarts automatically on crash, no manual intervention. (→1744)

3. **ToriChat gets smarter.** Semantic search is now a native ToriChat tool; the model can search your codebase directly. Tool call rounds raised from 6 to 15. (→1787, →1785)

4. **Faster Drive.** Drive service is now cached and cold-path metadata is parallelized — first load is noticeably quicker. (→1755)

5. **Specs get IDs.** Every spec now has a sequential ID (S001, S002...) in its frontmatter and the CLI. (→1784)

6. **Reliability fixes.** Vite 8 blank page on stale chunks (→1798), sidebar overlay/push regression (→1780), chat tab context bleed (→1782), async event-loop blockers across 3 backend routes (→1778, →1783).

7. **Jira inline comments.** You can now reply to Jira tickets directly from the Jira detail page inside yourOS. (→1772)

8. **Design guardrails tightened.** Pre-design-audit, brainstorming, and writing-plans skills now have project-local overrides that enforce the three-signal check and Create-vs-Existing distinction before any new file is written. (→1791, →1793, →1795–→1797)

---

## Summary

| Item | Finding |
|------|---------|
| Worktrees to delete on --apply | **0** |
| Part A recommendation | **Don't apply** (no value; dirty worktrees need triage first) |
| Commits on fix-1768-calendar-font | **65** |
| Files changed | 131 (+3198/-4487) |
| Dominant theme | needle→task rename (6 rename commits) + ~15 feature/fix merges |
| v4.1.0 cut readiness | Branch is ready to merge; no blockers found in audit |
