# v4.1.0 Frontend Regression Check

**Branch:** fix-1768-calendar-font  
**Date:** 2026-05-28  
**Agent:** e3-frontend-full-vitest-probe-c3688a  
**Merges covered:** 5-branch merge into fix-1768-calendar-font (including C onboarding refresh + Phase A needle→task rename)

---

## Results

| Metric | Count |
|---|---|
| Test files | 189 |
| Tests total | 2957 |
| Passed | 2954 |
| Failed | **2** |
| Skipped | 1 |
| Duration | 13.59s |

---

## Failures

### 1. `src/components/Layout.test.tsx`

**Test:** `Layout > chat resize layout reactivity > main content shrinks when chat grows so sidebar, main, chat all fit the viewport`

**Error:**
```
AssertionError: expected 'min-h-dvh min-w-0 overflow-x-hidden f…' to contain 'ml-56'
Expected: "ml-56"
Received: "min-h-dvh min-w-0 overflow-x-hidden flex flex-col transition-[margin] duration-200"
  at src/components/Layout.test.tsx:175:30
```

**Classification: PRE-EXISTING**  
This is the documented historically flaky chat-resize test. Last touched in `9f872d3c` (cover tour opt-in behavior), well before this merge. Not related to onboarding changes or Phase A rename.

---

### 2. `src/pages/Tasks.test.tsx`

**Test:** Optimistic delete — `'Delete me please'` expected to disappear after clicking delete button

**Error:**
```
await waitFor(() => {
  expect(screen.queryByText('Delete me please')).not.toBeInTheDocument()
})
// at src/pages/Tasks.test.tsx:2752:58
```

**Classification: PRE-EXISTING**  
Phase A rename (`200171d1`) touched 10 lines in Tasks.test.tsx — all label text changes (`Needles → Tasks`, `Threaded → Done`, etc.). Line 2752 was not touched by Phase A; it tests async optimistic deletion behaviour. The test queries `title="Delete needle permanently"` (not yet renamed), finds the button, clicks it, but the item doesn't disappear within the waitFor window — a timing/async race unrelated to the merge.

---

## Verdict

**YELLOW — pre-existing failures only, no merge-caused regressions**

Both failures existed before the 5-branch merge. The onboarding changes (`OnboardingWizard.tsx`, `OnboardingWizard.test.tsx`, `index.css`) and the Phase A 27-file rename introduced no new test breakage.
