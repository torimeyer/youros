# v4.1.0 Cut-Prep Gate Verification Report

**Date:** 2026-05-28  
**Branch:** fix-1768-calendar-font  
**Verifier:** d2-cut-prep-gate-probe agent

---

## Gate 1: Backend Health

**Status: GREEN**

```
curl --connect-timeout 3 -m 5 -k -s https://127.0.0.1:8000/api/health
{"status":"ok","service":"myos-api","checks":{"data_dir":true,"ostk":true}}
HTTP_STATUS:200
TIME:0.003249s
```

All checks pass (`data_dir`, `ostk`). Response in 3ms.

---

## Gate 2: Frontend Boots

**Status: RED (1 test failure)**

### tsc -b

```
TSC_EXIT:0
```

TypeScript compilation is clean. No type errors.

### Layout.test.tsx (vitest)

```
FAIL  src/components/Layout.test.tsx > Layout > chat resize layout reactivity
      > main content shrinks when chat grows so sidebar, main, chat all fit the viewport

AssertionError: expected 'min-h-dvh min-w-0 overflow-x-hidden f...' to contain 'ml-56'

Expected: "ml-56"
Received: "min-h-dvh min-w-0 overflow-x-hidden flex flex-col transition-[margin] duration-200"

at src/components/Layout.test.tsx:175:30

Test Files  1 failed (1)
      Tests  1 failed | 11 passed (33)
   Duration  735ms
```

**Blocker:** The chat-resize reactivity test expects `ml-56` on the main content element when the sidebar is open. The actual class list does not include `ml-56`. This is a real failure, not flakiness — the assertion is a hard string match against a class that is absent. 11 of 12 Layout tests pass.

---

## Gate 3: Tarball Gate (→1740)

**Status: GREEN**

The →1740 fix (`6db32e08: fix(→1740): update macOS platform name darwin-universal -> aarch64-apple-darwin`) is already merged into the branch. There is no separate hook for this gate. The gate lives in `scripts/test_release_conventions.sh` — a standalone pre-release script.

```
./scripts/test_release_conventions.sh

--- Version string consistency ---
  PASS  install.sh strips leading v from GitHub tag for filename
  PASS  install.sh tarball filename uses VERSION_NUMBER (no v)
  PASS  install.sh URL path uses raw tag VERSION (with v)
  PASS  install.sh does NOT build tarball name from raw tag (v-prefix bug)

--- Release notes filenames ---
  PASS  every docs/releases/*.md matches vN.N.N.md

--- Latest tag has matching release notes ---
  PASS  docs/releases/v4.0.0.md exists for latest tag

--- What's New entry ---
  PASS  releaseNotes.ts top entry is within 14 days (2026-05-28)

--- Live ostk tarball URLs ---
  checking version: v7.0.0 (filename uses 7.0.0)
  PASS  ostk aarch64-apple-darwin tarball HTTP 200
  PASS  ostk x86_64-unknown-linux-musl tarball HTTP 200

--- No hardcoded personal paths ---
  PASS  api/ has no hardcoded /Users/torimeyer paths (outside tests)
  PASS  app/src/ has no hardcoded /Users/torimeyer paths (outside tests)

--- Deprecated API field references ---
  (no deprecated fields tracked)

All 11 release-convention checks passed.
TARBALL_EXIT:0
```

No `--no-verify` flag needed. The gate passes clean on the working tree.

---

## Gate 4: Branch State

**Status: YELLOW (uncommitted files present)**

```
git log --oneline origin/main..fix-1768-calendar-font | wc -l
66

git status --short
 D .scaffold-1597
 D .scaffold-1600
 D .scaffold-1602
 D .scaffold-1618
 M app/package-lock.json
 M docs/draft/pattern-watcher-v2.md
 M docs/draft/user-memory-store-improvements.md

git diff origin/main..fix-1768-calendar-font --stat | tail -5
 scripts/backend_watchdog.sh                        |   10 +
 scripts/backfill_spec_ids.py                       |  112 +
 scripts/dev-frontend.sh                            |    6 +
 scripts/test_release_conventions.sh                |    2 +-
 131 files changed, 3206 insertions(+), 4495 deletions(-)
```

- 66 commits ahead of origin/main
- 4 scaffold deletes (`.scaffold-NNNN`) — expected cleanup, not a blocker
- `app/package-lock.json` modified but uncommitted — minor
- 2 docs/draft files modified — not release-blocking
- 131 files changed, 3206 insertions, 4495 deletions in the diff

No hard blocker here, but the working tree is not fully clean.

---

## Gate 5: NR-Enterprise Contamination

**Status: GREEN**

```
git log --oneline origin/main..fix-1768-calendar-font | grep -iE "nr|enterprise|new.relic"
(no output)
```

Zero matches. No NR/enterprise references in any commit subject on this branch.

---

## Gate 6: Smoke Test Coverage (Dry Run)

**Status: INFO (not run — awaiting A2)**

`scripts/e2e_smoke.sh` covers 6 phases:

1. **Backend pytest** — full `pytest -q --tb=short` suite
2. **Frontend vitest** — `pnpm test --run`
3. **TypeScript build** — `pnpm exec tsc -b`
4. **Live HTTP checks** — 15+ endpoint probes against the running API (tasks CRUD, agents, briefing, upgrade/status, fleet templates, workflow templates, settings, files, shares, knowledge, recurring tasks, threads)
5. **WebSocket round trip** — chat via `scripts/lib/e2e_ws_chat.py` (single-model and multi-model)
6. **Browser e2e** (optional, needs agent-browser)

Phases 4 and 5 require a running API server. The script detects HTTPS via self-signed cert presence and sets `RELEASE_MODE=1` to disable `--reload` during the run. A2 is responsible for executing this.

**Not covered by smoke test alone:** hook behavior, worktree hygiene, NR contamination (covered by Gate 5 above).

---

## Go / No-Go Recommendation

**NO-GO for v4.1.0 cut as of 2026-05-28.**

One hard blocker:

- **Gate 2 (Layout test):** `Layout > chat resize layout reactivity` is failing on tip. The `ml-56` class is absent from the main content element during a chat-resize scenario. This is not flakiness — it is a deterministic assertion failure. This must be fixed and confirmed green before cutting.

Everything else is ready:
- Backend healthy
- TypeScript clean
- Tarball conventions pass (all 11 checks)
- Branch is 66 commits ahead, 131 files changed, no NR contamination
- Working tree has minor uncommitted noise (scaffold deletes, package-lock, two draft docs) but nothing release-blocking

Fix the Layout test, re-run Gate 2, then the branch is cut-ready pending A2's full smoke run.
