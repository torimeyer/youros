# v4.1.0 NR-Enterprise Leak Check

**Branch checked:** `fix-1768-calendar-font`  
**Against:** `origin/main`  
**Date:** 2026-05-28  
**Verdict: CLEAN**

---

## Search Results

### Search 1 — All files on branch containing NR markers

```
git ls-tree -r --name-only fix-1768-calendar-font | xargs -I{} sh -c \
  'git show fix-1768-calendar-font:{} 2>/dev/null | \
  grep -liE "new[._-]?relic|newrelic|nr[._-]?enterprise|NREnterprise|NR_ENTERPRISE" \
  > /dev/null 2>&1 && echo {}'
```

**Output:** no matches

### Search 2 — Added/changed lines in diff for NR markers

```
git diff origin/main..fix-1768-calendar-font | \
  grep -iE "^\+.*(new[._-]?relic|newrelic|nr[._-]?enterprise|NREnterprise|NR_ENTERPRISE)"
```

**Output:** no matches

### Search 3 — Commented-out NR code in diff

```
git diff origin/main..fix-1768-calendar-font | \
  grep -iE "^\+.*(#|//).*new[._-]?relic"
```

**Output:** no matches

### Search 4 — Package manifests

```
git show fix-1768-calendar-font:app/package.json | grep -iE "newrelic|new-relic"
git show fix-1768-calendar-font:api/requirements.txt | grep -iE "newrelic|new-relic"
git show fix-1768-calendar-font:api/requirements-dev.txt | grep -iE "newrelic|new-relic"
```

**Output:** no matches in any manifest

### Search 5 — Env templates

```
git ls-tree -r --name-only fix-1768-calendar-font | grep "\.env" | \
  (while read f; do git show "fix-1768-calendar-font:$f" | grep -iE "NEW_RELIC|NR_" && echo "$f"; done)
```

**Output:** no matches

### Search 6 — Cross-reference: nr-enterprise commits in git log (all branches)

```
git log --all --oneline | grep -iE "nr.enterprise|nr_enterprise"
```

**Output:** 9 historical commits found — all on the `nr-enterprise` branch, which is a separate branch (internal NR repo). None appear in `git log origin/main..fix-1768-calendar-font`.

**Classification:** FINE — these are the expected isolated nr-enterprise branch commits. The `chore: remove nr-enterprise overlay infra (moved to internal NR repo)` commit confirms the overlay was deliberately moved off main.

### Verification — nr-enterprise commits NOT on fix branch

```
git log --oneline origin/main..fix-1768-calendar-font | grep -iE "nr.enterprise"
```

**Output:** no matches — confirmed zero nr-enterprise commits in the integration branch's diff.

---

## Diff summary

`fix-1768-calendar-font` is **ahead of `origin/main`** by commits covering:
- needle→task rename sweep (phases A–F)
- launchd KeepAlive, Jira comments, Vite 8 blank-page fix
- plain-language rules, semantic search tool, Drive cache
- calendar font fix (→1768)
- 131 files changed, 3206 insertions, 4495 deletions

None of these commits introduce NR content.

---

## Verdict

**CLEAN** — zero NR-related code, imports, configs, flags, comments, or strings exist on `fix-1768-calendar-font` that are not already on `origin/main`. Safe to merge for v4.1.0 cut.
