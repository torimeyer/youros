Text yourOS headlines this release: text a job in from your phone, confirm with YES, and get one text back when it finishes, all controlled from a new Settings card. The Agents and Sessions pages grew into a real mission control with per-session activity, stuck warnings, conflict alerts, and a daily digest. The feature-is-live popup now speaks plain language, tasks support select-all bulk actions, chat replies start faster with warm workers, and Gmail gets one-move bulk mark-as-read.

Reliability underneath: the text bridge no longer replies to itself, builds can no longer be marked finished with unsaved work, the full backend test suite is green (6,856 checks), and helpers are blocked from scanning private folders that triggered macOS permission popups.
