## What's new

### Live build demos

Two new live-build flows are wired so you can record yourself building inside myOS on camera.

- **Compose-the-jingle fanfare**: file a needle in torichat asking Gemini to compose a victory jingle. Gemini saves the `.mid` via the new fcp:midi rules. The dashboard plays the actual composition through Tone.js when you close any needle. Wired end-to-end. No pre-rendering, no fake audio.
- **All Claude tiers side by side**: the side-by-side toggle now fans out to Opus, Sonnet, Haiku, and Gemini in four labeled panes from a single prompt. Useful for "watch every model answer the same question" demos.

### UAT polish

Ten fixes landed from the 2026-05-20 UAT pass.

- **Husk labels renamed to "empty draft"** in the Specs UI. The word "husk" is gone from user-visible copy.
- **Kanban and Specs perf**: skips readiness checks for closed tasks and caches them in localStorage. Faster open, faster nav.
- **Gmail expired auth** now shows a "reconnect Gmail" button instead of an empty inbox.
- **Gem delete fix**: deleting a Gem used to crash on the 204 No Content response. Wrapped + actionable error copy added.
- **Messages page**: renders cached threads immediately, shows the right unread count, has a loading state. Top search box merged into the contact picker for one-place lookup.
- **torichat code blocks** now scroll inside the bubble instead of overflowing.
- **torichat reply routing**: in All-mode, replies go to the conversation you replied to, not the active one.
- **Drive slide deck preview** gracefully falls back to a "open in Drive" link on 401 auth expiry.
- **Tasks list** wraps intermittent JSON parse errors and surfaces them in the UI so you know what went wrong.
- **User chat bubbles** right-align regardless of length (the layout broke when the bubble was wider than the cap).

### ADHD-mode self-healing

ADHD mode used to require the model to manually arm a Monitor before every Agent spawn. That coupling was fragile: Monitor's pre-tool hooks take 200-500 ms, and the harness cancels any pending tool the moment you type. So every Agent spawn could die from a stray check-in.

- **Root cause traced and fixed**: ADHD's pre-agent check now auto-arms the watchdog itself instead of denying. The model no longer has to call Monitor for supervision to work. (See →1563 root-cause doc + commit `2c95ae4`.)
- **Backend-probe retry**: the pre-agent guard now retries on transient backend blips instead of denying outright.
- **ADHD pairing deny gives better hint**: when a deny does fire, the message suggests the canonical `monitor-agent.sh` helper.
- **Monitor "internal error" identified**: the harness emits "[Request interrupted by user for tool use]" when you type during a tool's pre-hook window. Surfaced as "Tool result missing due to internal error" in the UI. Not a bug, but documented so it stops being treated as one.

## Reliability

- **Vitest fully green**: cleaned up 164 pre-existing failures across 8 test files (→1568). All are MemoryPill needing Router context or stale mock shapes. 2767 tests pass, 1 skipped.
- **Pytest no longer hangs in test_1305**: lifespan shutdown now cancels 20+ background tasks (snapshot loop, merge-debt tick, etc) so AsyncClient(ASGITransport) tests stop leaking event-loop state (→1569). pytest-timeout switched to thread mode so async hangs actually die.
- **Ghost reaper PID check**: agents mid-tool-call no longer get reaped by the watchdog because of long bash waits (→1570).
- **ADHD monitor disarm on error + PostToolUse guard**: the ADHD enforcer now disarms cleanly when Monitor returns an error, rather than leaving stale state behind (→1571).
- **Slow spawn test no longer times out**: `@pytest.mark.timeout(90)` added to the test that hit the 30s default (→1572).

## Diagnoses landed

- **→1563 Monitor "internal error" root cause**: not a multi-tool-storm bug as previously hypothesized. It's user typing during the pre-tool hook window.
- **Tool cancellation pattern (bash + Monitor share the mechanism)**: report at `docs/draft/_review/2026-05-21-tool-cancellation-pattern.md`. Tier-1 fix landed: new standing rule that says use `mcp__ostk__spawn` for any bash command >10s, so user messages can't interrupt them.
- **→1587 agents look stuck**: root cause was the transcript file only captures plain-text model output, not tool calls. Long-running vitest via bash hit the 30s tool socket timeout. Subagent prompt template updated with three new lines: heartbeat current_step, use spawn for tests, skip pre-existing failures.

## Under the hood

- **Worktree cleanup pass-1**: triaged 251 worktree-agent branches, deleted 19 safely (12 redundant + 7 scaffold), tagged 61 under `refs/tags/archive/cleanup-2026-05-21/` for 90-day recovery, classified 179 unique-work branches for pass-2.
- **Gemini torichat now knows the fcp:midi rules**: 5-line addition to the Gemini system prompt tells it to save `.mid` only via fcp:midi, no shell-out to render, no brew, frontend audio under `app/public/sounds/`. Fixes a path where Gemini would over-reach and run rendering toolchains directly (→1583).
- **Standing rule 6**: bash commands expected to run >10s should use `mcp__ostk__spawn` + `mcp__ostk__interact`. The harness cancels pending tools when a user message arrives, and the cancellation window for bash is the entire execution time.

## Known open

- None of the 10 UAT needles remain open.
- Pass-2 worktree cleanup is in flight (179 branch deletions with archive tags).
- Open backlog: P1 →1465 (Plan Waves backend follow-on), P2 →1541 (cherry-pick →1458 ai_backend abstraction). Neither blocks v3.19.

---

*Diff: ~50 commits since v3.18.1.*
