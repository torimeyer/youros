# myOS v3.8.1

## Bug fix

**Content no longer hides behind the top bar.** A CSS shorthand (`sm:p-8`) was overriding the top padding on every page, causing content to sit behind the fixed navigation bar on wider screens. Fixed across all 21 page files.

## How to update

```
cd ~/myos
./update.sh
```
