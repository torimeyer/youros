// DependencyMapWidget — nested-list view of Jira issue dependencies (→1452)
// No new viz lib. Tree: epic at top, linked issues indented by direction.
export default function DependencyMapWidget({ epicKey }: { epicKey?: string }) {
  return <div data-testid="widget-dependency-map">Stub</div>;
}
