# Plan with mixed file references

Edits `api/services/spec_audit.py` (real file) and `api/routers/nonexistent.py` (does not exist).

## Acceptance criteria

- [ ] Implement changes in `api/services/spec_audit.py`.
- [ ] Handle edge cases in `api/services/spec_audit.py`.
- [ ] Add tests that cover both paths.
