# Agentfile alias. saa is Tori's muscle memory name for the
# comprehensive build pattern. Keep this file so existing callers,
# scripts, and memory notes that still say "saa" keep working.
# When the parser sees ALIAS <name>, it follows it to the target
# Agentfile and returns that config.

ALIAS builder
