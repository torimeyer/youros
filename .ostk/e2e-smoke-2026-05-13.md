# e2e smoke run — 2026-05-13

**Agent:** 1274-e2e-smoke-retry-with-spawn-62e301  
**Script:** `scripts/e2e_smoke.sh`  
**Exit code:** 1  
**Duration:** ~7 min  
**Verdict:** FAIL — 2 phases failed

## Summary

| Result | Count |
|--------|-------|
| PASS   | 138   |
| FAIL   | 2     |
| SKIP   | 1 (browser e2e — frontend not on port 3010) |

## Failures

### 1. pytest suite collection error (→1274)

**Phase:** Backend unit tests  
**File:** `api/tests/test_1271_agent_filters.py`  
**Error:**

```
ImportError while importing test module 'tests/test_1271_agent_filters.py'
ModuleNotFoundError: No module named 'api'
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 2.48s
```

The test imports `from api.services.agent_filters import is_user_spawned_agent` but the `api` package is not on `sys.path` during collection. Needle filed: →1274.

### 2. Tasks page first-paint budget exceeded (→1275)

**Phase:** Frontend unit tests  
**File:** `app/src/pages/Tasks.test.tsx`  
**Test:** `Tasks page - first-paint budget (needle 299) > first visible row arrives within 500ms on a warm backend`  
**Error:**

```
AssertionError: expected 928.7608330000003 to be less than 500
at src/pages/Tasks.test.tsx:2186:21
    expect(elapsed).toBeLessThan(FIRST_ROW_BUDGET_MS)
```

First row took 928ms, budget is 500ms. Needle filed: →1275.

## Passing phases

All of the following completed without issue:
- tsc -b (TypeScript build)
- Live HTTP checks (phase 4): all journeys, enterprise, CRUD, auth endpoints
- WebSocket chat round trips (phase 5): Claude, Gemini, Multi-AI
- E2E leftover regression
- Orphan reference sweep

## Last 30 lines of output

```
PASS  vite proxy targets use 127.0.0.1, not localhost
PASS  needles is a symlink (safe from git pull)
PASS  start.sh has valid bash syntax
PASS  start.sh has needles migration logic

=== WebSocket chat round trips ===

PASS  chat WS: Claude solo streams token + done
PASS  chat WS: Gemini solo streams token + done
PASS  chat WS: Multi-AI conversation streams

=== Browser e2e tests ===

SKIP  frontend not reachable on port 3010

=== E2E leftover regression ===

PASS  e2e leftovers: 0 specs remain after sweep

=== Orphan reference sweep ===

PASS  orphan-reference-sweep: every path referenced in e2e_smoke.sh exists on disk

=== Summary ===

PASS 138
FAIL 2
SKIP 1

2 phase(s) failed. Fix them before releasing.
```
