## →1270

**Old title:** Upstream: ostk binary generate_loadavg_line reads stale cached register — remove skip markers in test_loadavg_dispatch_call_site.py when binary is updated past 6.0.5

**New title:** Upstream

**Description preview:**
ostk binary generate_loadavg_line reads stale cached register — remove skip markers in test_loadavg_dispatch_call_site.py when binary is updated past 6.0.5

---

## →1334

**Old title:** →1270 pulled by execute-ostk-cache-phase-2-insta agent but blocked: ostk is at 6.0.5, task requires >6.0.5 upstream fix before skip markers can be removed from test_loadavg_dispatch_call_site.py. Phase 2 of ostk-cache is complete (hooks installed, proxy on launchd PID 80283). →1270 needs to be re-queued when ostk binary updates.

**New title:** →1270 pulled by execute-ostk-cache-phase-2-insta agent but blocked

**Description preview:**
ostk is at 6.0.5, task requires >6.0.5 upstream fix before skip markers can be removed from test_loadavg_dispatch_call_site.py. Phase 2 of ostk-cache is complete (hooks installed, proxy on launchd PID 80283). →1270 needs to be re-queued when ostk binary updates.

---

## →1424

**Old title:** Spec-auto-status Phase 2a: wrapper-launched claim path. Add mychat CLI subcommand 'myos chat --spec <slug> [--with gemini|claude]'. Wrapper POSTs /api/specs/{slug}/claim with source=wrapper before exec'ing the terminal CLI in the spec's working directory. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

**New title:** Spec-auto-status Phase 2a: wrapper-launched claim path

**Description preview:**
Add mychat CLI subcommand 'myos chat --spec <slug> [--with gemini|claude]'. Wrapper POSTs /api/specs/{slug}/claim with source=wrapper before exec'ing the terminal CLI in the spec's working directory. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

---

## →1425

**Old title:** Spec-auto-status Phase 2b: agent self-register claim path. When a terminal Claude or Gemini session is launched against a spec, its system prompt / preamble appends a curl instruction to POST /api/specs/<slug>/claim with source=agent once before starting implementation. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

**New title:** Spec-auto-status Phase 2b: agent self-register claim path

**Description preview:**
When a terminal Claude or Gemini session is launched against a spec, its system prompt / preamble appends a curl instruction to POST /api/specs/<slug>/claim with source=agent once before starting implementation. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

---

## →1426

**Old title:** Spec-auto-status Phase 2c: in-chat slash handler /build-spec <slug>. Chat router intercepts the slash command, resolves slug to a spec, calls /api/specs/<slug>/claim with source=slash attributed to the active chat session's agent. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

**New title:** Spec-auto-status Phase 2c: in-chat slash handler /build-spec <slug>

**Description preview:**
Chat router intercepts the slash command, resolves slug to a spec, calls /api/specs/<slug>/claim with source=slash attributed to the active chat session's agent. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

---

## →1441

**Old title:** Theme C v2 (v3.23.0) — Intel digest + spec promotion. Add GET /api/intel/digest?window_days=7 endpoint that synthesizes captures into a markdown digest and promotes to draft spec at ~/.youros/specs/intel-digest-{date}.md. Gated on the intel-synthesize skill landing in the plugin marketplace (separate task, not pclaude's job). Until skill lands, continue using inline LLM call via chat.py with TODO marker. Spec: docs/spec/narrative-coordination-intel-themes-main-only.md. Blocked by Theme C MVP.

**New title:** Theme C v2 (v3.23.0) — Intel digest + spec promotion

**Description preview:**
Add GET /api/intel/digest?window_days=7 endpoint that synthesizes captures into a markdown digest and promotes to draft spec at ~/.youros/specs/intel-digest-{date}.md. Gated on the intel-synthesize skill landing in the plugin marketplace (separate task, not pclaude's job). Until skill lands, continue using inline LLM call via chat.py with TODO marker. Spec: docs/spec/narrative-coordination-intel-themes-main-only.md. Blocked by Theme C MVP.

---

## →1465

**Old title:** Plan Waves backend pieces: /api/agents/spawn-preflight endpoint + builder-prompt agent-side conflict re-check (follow-up to →1461 frontend rebuild).

Frontend rebuild landed in commit bd04785 (merge of worktree-agent-plan-waves-frontend-b-e1d738e3). PlanWavesPanel + Specs.tsx already call lib/spawn.buildSpec which handles 409 lock_conflict via ConflictDialog. The kernel returns 409 today only when the actual spawn collides. The original plan added two more layers:

1. NEW endpoint GET /api/agents/spawn-preflight?paths=... in api/routers/agents.py. Returns the same {conflicts:[...]} shape as the 409 response but without spawning. Refactor _acquire_spawn_locks (line 4442) to extract _compute_path_conflicts(paths) so preflight and acquire share it. Always 200.

2. Update the builder-spawn prompt in api/routers/specs.py (the prompt builder used by POST /specs/{path}/build) to prepend a mandatory first step: 'Before reading or editing anything, GET /api/agents to list active agents. If any agent (other than yourself) is holding any path in your spawn locks, exit cleanly with a message naming the conflicting agent. Do not retry.'

3. Update lib/spawn.buildSpec to call /spawn-preflight before POSTing /specs/{path}/build (frontend). If preflight returns conflicts, surface ConflictDialog without a spawn attempt.

4. Test coverage: api/tests/test_agents.py::test_spawn_preflight_no_conflict + test_spawn_preflight_with_conflict. api/tests/test_specs.py::test_builder_prompt_includes_recheck_instruction.

Original plan file: /Users/torimeyer/.claude/plans/let-s-add-a-build-abstract-wreath.md. Wait for claude-code-4285 and the agent-build-1458-ai-backend worktree to finish editing api/routers/agents.py and api/routers/specs.py before starting; both files are hot.

**New title:** Plan Waves backend pieces

**Description preview:**
/api/agents/spawn-preflight endpoint + builder-prompt agent-side conflict re-check (follow-up to →1461 frontend rebuild).

Frontend rebuild landed in commit bd04785 (merge of worktree-agent-plan-waves-frontend-b-e1d738e3). PlanWavesPanel + Specs.tsx already call lib/spawn.buildSpec which handles 409 lock_conflict via ConflictDialog. The kernel returns 409 today only when the actual spawn collides. The original plan added two more layers:

1. NEW endpoint GET /api/agents/spawn-preflight?paths=... in api/routers/agents.py. Returns the same {conflicts:[...]} shape as the 409 response but without spawning. Refactor _acquire_spawn_locks (line 4442) to extract _compute_path_conflicts(paths) so preflight and acquire share it. Always 200.

2. Update the builder-spawn prompt in api/routers/specs.py (the prompt builder used by POST /specs/{path}/build) to prepend a mandatory first step: 'Before reading or editing anything, GET /api/agents to list active agents. If any agent (other than yourself) is holding any path in your spawn locks, exit cleanly with a message naming the conflicting agent. Do not retry.'

3. Update lib/spawn.buildSpec to call /spawn-preflight before POSTing /specs/{path}/build (frontend). If preflight returns conflicts, surface ConflictDialog without a spawn attempt.

4. Test coverage: api/tests/test_agents.py::test_spawn_preflight_no_conflict + test_spawn_preflight_with_conflict. api/tests/test_specs.py::test_builder_prompt_includes_recheck_instruction.

Original plan file: /Users/torimeyer/.claude/plans/let-s-add-a-build-abstract-wreath.md. Wait for claude-code-4285 and the agent-build-1458-ai-backend worktree to finish editing api/routers/agents.py and api/routers/specs.py before starting; both files are hot.

---

## →1399

**Old title:** mychat: background work (ScheduleWakeup / Monitor) — chat can park a request, monitor an external event (CI, deploy, agent), and resume the conversation when something changes

**New title:** mychat

**Description preview:**
background work (ScheduleWakeup / Monitor) — chat can park a request, monitor an external event (CI, deploy, agent), and resume the conversation when something changes

---

## →1400

**Old title:** mychat: ostk audit / undo trail in chat — show file changes the model made this turn as collapsible diffs with a per-change Undo button; uses ostk gen_table + audit rows

**New title:** mychat

**Description preview:**
ostk audit / undo trail in chat — show file changes the model made this turn as collapsible diffs with a per-change Undo button; uses ostk gen_table + audit rows

---

## →1427

**Old title:** Spec-auto-status Phase 3: passive git-commit detection. Install ostk_post_commit_hook.sh during 'ostk boot' that diffs commit messages for spec slugs (against ~/.youros/specs/*.md) or →NNNN task IDs. On match, POST /api/specs/<slug>/claim with source=passive attributed to commit author. Fallback: 60s cron tick scanning recent commits if hook not installed. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

**New title:** Spec-auto-status Phase 3: passive git-commit detection

**Description preview:**
Install ostk_post_commit_hook.sh during 'ostk boot' that diffs commit messages for spec slugs (against ~/.youros/specs/*.md) or →NNNN task IDs. On match, POST /api/specs/<slug>/claim with source=passive attributed to commit author. Fallback: 60s cron tick scanning recent commits if hook not installed. Source spec: ~/.youros/specs/spec-auto-status.md. Blocked by: Phase 1 backend.

---
